# @codeforge/design-tokens

Shared cyan/teal design tokens for CodeForge clients.

- Import CSS: `@import "@codeforge/design-tokens/tokens.css"` or `@codeforge/design-tokens/tokens.css`
- Use `--cf-*` variables in all new styles; legacy `--primary` aliases remain during migration.
