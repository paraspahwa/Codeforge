export const metadata = {
  title: "About — CodeForge",
  description: "About CodeForge — India-first AI product partner.",
};

export default function AboutLayout({ children }) {
  return children;
}
